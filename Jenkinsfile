pipeline {
    agent any

    stages {
        stage('myfirstfile') {
            steps {
                echo 'testing a pipeline project'
            }
        }
    }
  
  stages {
        stage('testing file') {
            steps {
                echo 'checking the file we created'
            }
        }
    }
}
